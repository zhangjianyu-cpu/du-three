import { defineConfig } from 'dumi';

export default defineConfig({
  outputPath: 'docs-dist',
  themeConfig: {
    name: 'du-three',
    logo: '/logo.png',
    footer: 'welcome to du-three',
    socialLinks: {
      github: 'https://github.com/zhangjianyu-cpu/du-three/tree/main',
    },
  },
});
